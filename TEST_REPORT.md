# 詞織测试报告

## 2026-09-03：片假名学习、移动排版与单次查询优化

### 范围与验收

- 只调整结果字段、展示、单次 enrichment prompt 与容错边界；未修改 Romaji/IME 解析器、数据库、认证、30 天 Session、Owner 或体验码结构。
- 自动回归：**144/144**，12 个测试文件；typecheck、lint、production build 通过。覆盖 Optional 字段、数组/嵌套 JSON、去重、显示默认值、轻量 reasoning、原有活用/并发/额度/Owner 安全测试。
- 正式网址：`https://shiori-words.vercel.app`。同一固定语料前后各 **40 条未命中应用缓存的真实查询**：15 日语/Romaji、15 片假名、5 中文、5 英文/简单句。另保留中间两轮各 40 条失败调查数据，以及最终 38 条浏览器验收（20 片假名 + 12 Romaji 写法 + 3 开关词 + 3 长标题）。不是只测 mocked provider。
- 最终片假名 20/20：主词、中英释义、来源/构成区分、例句均可用；コンセント保守省略不确定词源；コーヒー、アルバイト显示荷兰语/德语来源。类型标签不视为学术词源鉴定。
- 12/12 Romaji 变体得到正确主词与释义；个别来源字段仍可能被模型省略或误填，详见限制，不把它算作完整词源准确率。
- 学校、こんにちは、モバイルバッテリー、インターネット、語彙プリント：默认隐藏、点击显示、再次收起、键盘操作通过；刷新后偏好恢复。仅本地 boolean UI 偏好，不存认证数据。

### 同语料 Production 性能

测量基线 `35f8136` 与最终推理配置 `7e7e586`；后续修复只影响字段恢复、展示和追问上下文，普通查词 prompt 未再变化。Owner 主动开启测量时才绕过应用结果缓存，仍保留 Provider 自身 prompt cache；没有禁用限流。两轮顺序运行于不同时段，网络与冷启动会影响数字，不当作严格因果实验。AI 使用完整 JSON 响应，无法取得可信 TTFT。

| 指标 | 优化前 Median / P95 | 优化后 Median / P95 |
| --- | ---: | ---: |
| 首个本地结果（浏览器） | 177 / 509 ms | **161 / 458 ms** |
| AI 请求至完整响应 | 2.377 / 3.423 s | **1.521 / 2.638 s** |
| 浏览器端到端 | 2.636 / 4.680 s | **1.870 / 3.102 s** |

优化后平均 AI 1.628 s，平均端到端 1.967 s。首个本地结果最慢 2.654 s，冷启动仍存在。

| 优化后分类 | 条数 | 本地 Median / P95（ms） | AI Median / P95（ms） | 端到端 Median / P95（ms） |
| --- | ---: | ---: | ---: | ---: |
| 日语 / Romaji | 15 | 170 / 2654 | 1231 / 2638 | 1503 / 3770 |
| 片假名 | 15 | 192 / 243 | 2081 / 2642 | 2264 / 2795 |
| 中文 | 5 | 163 / 211 | 1231 / 1446 | 1468 / 1586 |
| 英文 / 简单句 | 5 | 140 / 145 | 1721 / 2793 | 1961 / 2941 |

小样本分类的 P95 接近最慢值，不能代表长期分布。

### Token 与调用次数

| 每次查询 | 优化前平均 | 优化后平均 | 优化后 Median / P95 |
| --- | ---: | ---: | ---: |
| Input | 354.28 | **286.73** | 260 / 366 |
| Cached input（包含在 Input 内） | 153.60 | **80.00** | 128 / 256 |
| Output | 307.23 | **169.28** | 156.5 / 246 |
| Total | 661.50 | **456.00** | 411.5 / 615 |
| AI calls | 1.00 | **1.00** | 1 / 1 |

40 条共 18,240 tokens；均值折算 100 条约 45,600 tokens。相比同语料基线减少约 31%，也低于旧 150 条报告的 731 tokens/query；语料不同，不直接拿旧数字计算提升率。缓存命中量不是成本之外的额外 token。

基线已禁用 thinking，本次提速主要来自缩短已知事实与输出、默认 1–2 例句，而不是“刚刚关闭深度思考”。普通词、Romaji 和简单句继续 non-thinking；长输入（>240 字符）或带上下文的细微语法/用法追问才启用 low reasoning。复杂度本地判断，没有额外分类请求。

### 真实移动排版与交互

- Chrome 实际渲染 CSS viewport：390×844、393×852、430×932、768×1024、1024×768、1440×900，均截图检查，无横向溢出。
- モバイルバッテリー在三个手机宽度为 36px、单行；来源、自然英语、中文与说明均进入首屏。iPad/桌面长标题 48px。学校保留较大字号。更长的コンピューターサイエンス自然分为两行，没有小假名/长音符单独起行。
- Aa 为语义 button，44×44px，支持 aria-pressed/expanded、键盘与 focus-visible；所有读音偏好只影响 UI。移除完成请求后延迟滚动，避免用户开始下一次输入时页面忽然移动。
- 这是 Chrome viewport 验证，不冒充物理 Android/iPhone/iPad 或 Safari 真机、软键盘测试。原生设备差异仍需使用反馈。

### 保留的失败与修复边界

1. 中间版本一次 **kitte 的真实 Provider 超时**：AI 25.004 s、端到端 27.939 s；基础结果保留，加载正常结束。该条仍保存在中间 40 条数据中，未删除；usage 未返回，不捏造 token。最终复测该词正常，但外部超时不可能被保证消失。
2. モバイルバッテリー曾被标为“直接借词”。对“构成与自然英语不同却声明未变义借词”的矛盾结果，去掉不可信分类并显示谨慎提示；不在本地硬猜和制英语。
3. クレーム的模型 naturalEnglish 曾为 claim；已有审核注释现在优先于新生成字段，保留 complaint/customer complaint。没有新增针对测试词的解析字典。
4. アルバイト曾返回泛化的 work/job，マンション曾把 mansion 当自然英语。Prompt 明确要求现代**日语含义**对应的英语，最终复测分别为 part-time job、apartment building/condominium。
5. スマホ曾缺来源与例句；Provider 把可选字段嵌入 dictionary 或与 output wrapper 混用时，现在逐字段恢复，不因根字段已存在就丢弃其他字段。
6. 一次追问返回 answer/explanation 数组，被旧恢复路径当成整段 JSON。已添加数组对象/answer 恢复、明确追问输出形状，且带入片假名上下文；不重新调用 AI 修 JSON。
7. sumaho 的来源字段曾误填日语展开式スマートフォン。此类非外语来源值会被省略，保留自然英语和说明；未冒充来源字段全部可靠。
8. 词源分类/措辞仍由模型补充，可能有波动；有疑问应核对权威词典。保守省略比确定地展示错误信息更安全。
9. 罗马字仍沿用现有转写习惯（如 gakkou、konnichiha、mobairubatterii），未改核心转写为带长音符、按发音分词的 Hepburn 系统。

最终 40 条测量无无限 loading、白屏、未捕获异常，均 1 次 AI 调用。加载保护仍为 Provider 25 秒、客户端流空闲 30 秒；超时保留基础结果并提示，不自动追加 AI 调用。测试期间浏览器自动化的过早输入/旧结果读取、截图窗口状态失配样本单独隔离，不混进性能统计，也不计为产品成功。

---

## 历史：2026-08-30 核心查询黑盒测试

测试日期：2026-08-30

## 结论

- 共执行 **557 条本地黑盒查询**、**150 条真实 DeepSeek 端到端查询**、**300 次请求生命周期压力模拟**，合计 **1,007 个运行场景**；另有 **79 个 Vitest 测试**。
- 306 条 Romaji/IME/活用输入：Top-1 **95.75%**，Top-3 **99.35%**，完全无结果率 **0%**。
- 120 条合法活用/口语形的 False Correction Rate 为 **0%**；6 条标注为需要纠错的样本均在 Top-3，Fuzzy Correction Precision **100%**。
- `aishiteru` 稳定解析为 `愛してる（あいしてる）`，没有再被低置信 fuzzy 候选覆盖。
- 150 条真实请求全部成功，无空结果、schema 异常或未捕获异常；每次查询实际 AI 调用数恒为 **1**。

## 测试覆盖

| 本地黑盒类别 | 数量 | 结果 |
| --- | ---: | --- |
| 随机 JMdict reading → Romaji 反查 | 150 | Top-1 150/150；Top-3 150/150 |
| 随机动词/形容词活用与口语缩约 | 120 | Top-1 108/120；Top-3 118/120；无错误强制纠正 |
| Romaji / 日本 IME / 长音 / 错拼 | 36 | Top-1 35/36；Top-3 36/36 |
| 直接日语 | 80 | 精确词条 Top-1 68/80；Top-3 78/80；其余进入可用 fallback |
| 中文 | 59 | 59/59 有稳定基础结果或 AI fallback；语言检测无异常 |
| 英文 | 52 | 52/52 有稳定基础结果或 AI fallback；语言检测无异常 |
| 混合脚本/连续无空格 | 30 | 30/30 无空白、崩溃或乱码 |
| 空输入、数字、emoji、全角、超长与多行 | 30 | 30/30 无异常或无限 loading |

真实 API 分层样本包含 95 条 Romaji/活用、24 条日语、17 条中文、13 条英文和 1 条混合输入。150/150 通过结构与完整性 sanity checks；另人工审查了 73 个分层结果，保留的语义歧义见后文。

## 稳定性与请求次数

- 300 次生命周期模拟覆盖：快速连续输入、AbortController 取消、旧请求返回、网络延迟、Provider timeout、坏 JSON、缺字段、空 examples、纯文本和损坏 NDJSON 行。
- 白屏：0；空结果：0；未捕获异常：0；永久 loading：0；旧请求覆盖新请求：0。
- 体验码事务、失败归还与 50 路并发下 20 次原子上限测试通过；一次主查询只扣一次，词典查询与内部解析不扣次数。
- 真实 API calls/query：平均 **1.00**，P95 **1**，最大 **1**；发生 `>1` 次调用的查询为 **0**。
- 私人实例测试覆盖：错误初始化令牌、唯一 Owner、Owner 不计人数、关闭注册、并发人数上限、注册邀请码原子计数、停用/恢复、Session 强制注销、普通用户级联删除，以及 Owner 不可停用/删除；全部通过。

## Token 与费用

150 条真实请求的 Provider usage：

| 指标 | 平均 | Median | P95 | 最大 |
| --- | ---: | ---: | ---: | ---: |
| Input tokens | 396.81 | 407 | 494 | 566 |
| Cached input tokens | 11.09 | 0 | 0 | 384 |
| Output tokens | 334.24 | 330 | 547 | 900 |
| Total tokens | 731.05 | 751 | 907 | 1,225 |

按当前 `deepseek-v4-flash` 官方价格估算：

| 查询量 | Off-peak | Peak |
| ---: | ---: | ---: |
| 100 次 | 约 **US$0.031** | 约 **US$0.061** |
| 1,000 次 | 约 **US$0.306** | 约 **US$0.611** |
| 10,000 次 | 约 **US$3.06** | 约 **US$6.11** |

价格基于 [DeepSeek 官方定价](https://api-docs.deepseek.com/quick_start/pricing/)，缓存 token 按实际 usage 分开计算。旧链路没有完整 usage 记录，因此不虚构历史百分比；可确认的变化是主查询从可能的 2～3 次补全调用收敛为最多 1 次，JSON 修复不再发起第二次 AI 请求。

## 性能

| 阶段 | 平均 | Median | P95 | 最慢 |
| --- | ---: | ---: | ---: | ---: |
| 本地规范化 + 词典 + 候选（557 条热态） | 1.63 ms | 0.38 ms | 6.43 ms | 30.70 ms |
| 真实请求中的词典阶段 | 24.87 ms | 1.31 ms | 65.96 ms | 413.04 ms |
| DeepSeek AI 阶段 | 3.07 s | 3.11 s | 4.15 s | 5.33 s |
| 真实查询端到端 | 3.16 s | 3.15 s | 4.38 s | 5.35 s |

采用完整结构化响应优先保证稳定性，Provider 本身未开启 token streaming，因此不能提供可信的 Provider TTFT。浏览器仍先收到本地基础结果；在单独 20 条流式事件测量中，首个可见 NDJSON 事件 Median **29.87 ms**、P95 **985.97 ms**。

## 根本原因与修复

- 原查询按中文、日语、英文和 Romaji 分散处理；部分路径只有小词表，词典未命中时缺少统一 fallback。
- 未完成的流式 JSON、严格的全字段 schema 和 Optional 字段形状变化会让已经返回的内容整体丢失。
- 旧 fuzzy 只看普通编辑距离，词典 headword 会错误覆盖合法活用、口语缩约和可直接解释的 Kana。
- 前端并发请求缺少完整的 request-id/abort 约束，旧响应可能覆盖新查询。
- 现在统一为 `normalize → classify → Japanese resolve → JMdict → 单次 AI enrichment → partial-safe schema → UI`；严格 JSON、修复 JSON、纯文本 fallback 均无需第二次模型调用。
- 候选排序改为 `exact surface > exact reading > morphology > common orthography > high-confidence correction > low-confidence suggestion`；低置信候选只作为“可能想输入”，不能覆盖原始合法读音。

## 失败案例 Top 10

这些是质量边界，不是崩溃或空结果：

1. `tokashimasu`：预期 `梳かします`，词形候选 Top-2；孤立读音存在同音歧义。
2. `tsumetakatta`：预期 `冷たかった`，Top-1 为同音的 `詰めたかった`，预期项 Top-2。
3. `tsukitai`：预期来自 `点く`，候选 Top-4；同音基本形过多。
4. `tsukimasu`：预期来自 `吐く`，候选 Top-5；无语境无法可靠区分多个 `つく`。
5. `kakushitai`：预期来自 `隠す`，Top-1 为同音形 `画したい`，预期项 Top-2。
6. `intaanet`：正确 `インターネット` 在 Top-3，但本地分段 Top-1 仍可能含未解析尾字母；AI 端可解释，尚未强行自动替换。
7. `明けましておめでとうございます`：紧凑 JMdict 无整句 headword，正确进入 AI fallback。
8. `register`：孤立英文既可表示“登记”也可表示“收银机”，一次真实结果优先了 `レジ`；需要目标语言或语境消歧。
9. `itte`：`行って / 言って` 同音；系统保留候选，不将任一低置信选择冒充唯一事实。
10. `matteru / wakatteru`：形态合法但基本形候选可能并列；系统优先保留精确读音，由 AI 结合语义选择常见表记。

## 已知问题

- 仓库只打包 22,636 条 JMdict 常用词紧凑索引；罕见词、专名和完整中文释义仍依赖 AI。
- 孤立同音活用无法在没有上下文时保证唯一 Top-1；为避免过度纠错，系统宁可展示候选。
- 公共词汇缓存是单进程 LRU，多实例部署不共享缓存，但不会影响正确性或隐私。
- 真实 API 的混合输入样本数少于其他类别；30 条混合输入已全部由本地黑盒覆盖，后续可扩大真实语义抽样。
- DeepSeek 网络与模型生成是端到端耗时和少量措辞波动的主要来源。

## 最终验证

`npm run qa:local`、`npm test`、`npm run typecheck`、`npm run lint`、`npm run build` 均通过；`npm audit --omit=dev` 无已知生产依赖漏洞。浏览器实测 Desktop、`390×844`、`768×1024`、`1024×768`，无横向溢出或控制台 error/warning；移动端“目标语言”逐字换行问题已在实测中发现并修复。
