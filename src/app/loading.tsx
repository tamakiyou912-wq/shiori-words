export default function Loading() {
  return (
    <main className="page-content route-loading" aria-live="polite" aria-busy="true">
      <div className="route-loading-line"><span />正在打开页面…</div>
    </main>
  );
}
